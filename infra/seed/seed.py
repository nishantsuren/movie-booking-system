"""Seed data tooling — Phase 0 stub.

Empty at this stage because there's no schema to seed yet (catalog and
theatre tables land in Phase 1). This file exists now so the location and
the pattern are established before there's real data to load: each
future addition here should be idempotent (safe to run repeatedly against
an already-seeded database) and should target the service databases
directly, the same way a real migration/seed tool would in any
environment, local or otherwise.
"""

if __name__ == "__main__":
    print("No seed data yet — schema lands in Phase 1. This script is a placeholder.")
